# Review — T00 Walking skeleton

- **Branch:** `task/T00-skeleton` (6 commits, `9a9d2ce`..`ee9fe14`)
- **Reviewed:** 2026-09-10
- **`just check`:** ✅ pass — reproduced locally from `just setup` (ruff format+lint, mypy --strict 16 files, pytest 8 passed w/ real Postgres via testcontainers, eslint strictTypeChecked, tsc, vitest 4 passed, `vite build`). Exit 0.
- **Extra verification:** `just up` → Postgres healthy; `alembic downgrade base` then `upgrade head` clean from empty; `pcs stdio` initialize + `tools/list` → exactly `{register_project, set_current_focus, get_project_briefing}`; `/api/health` reports `bind_host: 127.0.0.1`.
- **Verdict:** ✅ **approved** — merged with one reviewer-applied fix (F1). Remaining items are follow-ups for T01, not blockers.

## Requirement coverage

| ID | Verified by | OK |
|----|-------------|----|
| FR4 (Postgres store) | `alembic/versions/0001_baseline.py`, integration test on real PG | ✅ |
| FR5 / FR9 (briefing, token cap) | `context/service.get_project_briefing` + `cap_to_tokens`; `test_briefing_round_trip_*` | ✅ (real §7.2a is T01) |
| FR8 / D3 / AC16 seed (explicit project, error lists projects) | `resolve_project` → `ProjectNotFoundError`; `test_*unknown_project*` (service + MCP + HTTP 404) | ✅ |
| FR14 (register) | `service.register_project`; seeds overview entry | ✅ |
| NFR3 (compose + stdio + HTTP) | `deploy/docker-compose.yml`, `pcs stdio` / `pcs http`, `build_http_app` mounts `/mcp` + `/api/*` | ✅ |
| NFR6 (per-call audit log) | `logging.log_tool_call`; every tool + `/api` handler calls it | ⚠️ mechanism present, not asserted by a test — see S2 |
| NFR14 (localhost bind) | `config.bind_host` hard-returns `127.0.0.1`; `tailscale` raises `NotImplementedError` (T08 seam); compose binds `127.0.0.1:5432` | ✅ |
| FR39a / NFR15 / AC27 seed (token-driven UI) | `web/src/styles/tokens.css` verbatim from DESIGN.md; grep shows no literal hex/px in components; components map to documented DESIGN.md parts | ✅ |
| D1 (one datastore) | single PG; no SQLite/files | ✅ |

## Findings

### Reviewer-applied fix (F1)
- **F1** — no Python version pin. `uv` resolved **3.14.7** locally; `pyproject.toml` sets `requires-python = ">=3.12"` and mypy targets `3.12`; CI's `setup-uv` would pick yet another. Dev / CI / type-check target can all diverge. **Fix applied on the branch before merge:** added `server/.python-version` = `3.12` and a `uv python install` step note. Trivial, no logic touched.

### Should (follow-ups — assign to T01)
- **S2** — NFR6 has no test. `configure_logging()` only runs in `__main__`; under pytest the `pcs` logger has no handler so `log_tool_call` INFO lines are dropped silently. T01 should add a test that captures the audit record for a tool call (assert tool/project/caller/outcome present).
- **S3** — `pyproject.toml` `[tool.ruff] extend-exclude` and `[tool.mypy] exclude` drop the whole `alembic/` tree. Fine now, but every future migration file (T01+) will be unchecked. T01: narrow the exclude to `alembic/env.py` + `script.py.mako`, keep `versions/*` linted+typed.

### Nits
- **N1** — `test_integration.py:106-107` calls `client.get(.../briefing)` twice (leftover); drop the first.
- **N2** — `.gitignore` gained a `web/node_modules/.tmp/` line under a new "build info" section — harmless, slightly redundant with `node_modules/`.
- **N3** — two pytest deprecation warnings (Starlette TestClient→httpx2; anyio alias). Upstream, warnings only. Leave until they bite.

## Accepted deviations (from the agent's handoff)
- **`mcp>=1.13,<2`** — correct: FastMCP is 1.x; 2.x is a rename + transport change and should be a deliberate future task. ✅
- **Two extra `/api` routes** (`POST /api/projects`, `GET /api/projects/{p}/briefing`) — justified; the browser must not embed an MCP JSON-RPC client. T06 owns the real frontend API surface. ✅
- **`uv_build` build backend** instead of hatchling — no functional impact, native src-layout. ✅
- **`testcontainers[postgres]`** extra name (task said `[postgresql]`) — matches the 4.x package. ✅

## Notes for Phase 1
- The service-layer / thin-transport split is the pattern (see `AGENTS.md` "Notes affecting T01–T09" in the handoff). T01 must not put SQL in `pcs.mcp`.
- FastMCP tools take `ctx: Context | None = None` so `mcp.call_tool()` works in tests — keep it.
- `mcp.custom_route` attaches non-MCP routes to the same Starlette app — T06/T08 need no second web framework.
- testcontainers pulls `pgvector/pgvector:pg16` (~150 MB) on a cold runner — budget CI timeouts.
- `context_entries` is intentionally flat; T01 replaces it (headline/detail rules, priority, audit revisions, per-section shape, expiry) via new migrations.
