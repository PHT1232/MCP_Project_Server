"""Project Context MCP Server (`pcs`).

Walking-skeleton package (task T00). Establishes the conventions every later
component follows: config via :mod:`pcs.config`, DB wiring in :mod:`pcs.db`,
component logic in its own module (unit-tested without MCP), and the MCP surface
assembled in :mod:`pcs.mcp`.
"""

__all__ = ["__version__"]

__version__ = "0.0.0"
