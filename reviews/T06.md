# Review — T06 Frontend shell + context dashboard

- **Branch:** `task/T06-frontend-shell` (7 commits) — **rebased by the reviewer** onto current `main` (was based on `10fd175`, pre-T02/T04); one trivial `ROADMAP.md` conflict.
- **Reviewed:** 2026-09-10
- **`just check`:** ✅ pass post-rebase — server ruff + mypy `--strict` (61 files) + pytest 87/1-skip; web **eslint clean, tsc clean, vitest 26 passed, `vite build`**; `compose-lint`. Exit 0.
- **Verdict:** ✅ **approved — merged.** All `web/`-only; no server changes. A few UI-polish notes for T07's visual pass.

## Requirement coverage

| ID | Verified by | OK |
|----|-------------|----|
| FR30 (SPA served same-process) | uses T08's `PCS_STATIC_DIR` + SPA catch-all; agent ran it live | ✅ |
| FR31 (project picker + one-line status) | `ProjectPicker` / `ProjectList` from `GET /api/projects` | ✅ |
| FR36 (context dashboard: create/update/resolve) | `SectionPanel` / `EntryForm` / `EntryCard` → `POST\|PATCH /entries`, `POST …/resolve`; focus via `PUT …/focus` (`set_current_focus` replace-semantics — S-T06 honoured, generic `add_focus` not surfaced) | ✅ |
| FR36a (requirements view: N/M done, add, status) | `RequirementsView` — `listRequirements` → `doneSummary(done_count, total_count)` (clamped); add/status via entry routes; "Sync file" → `SyncReport`; surfaces `requirements_file.errors` / `reconciliations` | ✅ |
| FR37 (index panel + reindex) | `IndexView` — counts, build times, skipped-with-reason, "Reindex now" / "Full rebuild" → `POST …/reindex` | ✅ |
| FR38 / D6 (manual refresh, no push) | `useRefreshAll` invalidates the current project's queries; `refetchOnWindowFocus:false`; no polling/sockets; `DashboardEdit.test.tsx` asserts a second fetch only after an edit | ✅ |
| FR39a / NFR15 / **AC27** | grep: **no hex/`px` style literals outside `tokens.css`** (only doc-comments). `StatusBadge` is outline-only (no filled chromatic buttons); Signal blue never used for status; Calibre-only; 99px/16px radii via `rounded-buttons`/`rounded-cards`; components map to documented DESIGN.md parts | ✅ (see N1) |
| AC14 (frontend edit ↔ next briefing) | `DashboardEdit.test.tsx` (edit → refetch); briefing panel re-queries on mutation invalidation; agent confirmed live both directions | ✅ |
| AC14a (requirements N/M + store↔file) | `RequirementsView` + `requirements.test.ts` (`doneSummary`); write responses' `requirements_file` surfaced | ✅ |
| AC21 (semantic-unavailable indicator) | `lib/semantic.ts` `semanticIndicator` — reads `semantic_available` + `semantic_note` (real T04 fields) with a default reason; `semantic.test.ts` | ✅ |

## Notes for T07 (visual pass) — not blocking

- **N1** — the Requirements view renders several `RequirementStatusBadge`s on one
  screen (one per requirement, each in its own bordered sub-panel — the agent
  modelled it on DESIGN.md's "Insider Transaction Card" one-pill-per-row). Defensible
  (the list is not a single card), but confirm it reads right in T07's 6-pillar
  visual check — DESIGN.md's "≤1 chromatic accent per card" could be read stricter.
- **N2** — Overview can only be *edited*, not *created*, from the UI (register
  always seeds one). Fine unless a project's overview is ever deleted.
- **N3** — entry history (`GET …/entries/{id}/history`) is not surfaced (FR36 lists
  only create/update/resolve). A "history" affordance is a reasonable T07/polish add.

## Accepted deviations
- **No server changes** — the T01–T03 HTTP surface had everything needed. ✅
- Hand-rolled History-API router (`web/src/router/`) instead of `react-router` —
  keeps runtime deps at zero; handles popstate, modifier-click passthrough,
  scroll reset. ✅
- New **dev** deps `@testing-library/react` + `@testing-library/dom` (render test). ✅

## Seams for T07 (from handoff)
- `views/CodeMapView.tsx` stub at `/projects/:p/code-map`, already in the nav.
- `search` route exists but needs a `searchCode()` client fn + a results UI.
- The design-system components (`Card`, `PillButton`, `PillNav`, `StatusBadge`,
  `Typography`, `Callout`, `EntryCard`, `fields`) are the toolkit — reuse them.
- Router: `useNavigate()` / `<Link to>` from `web/src/router/`.
- All new server routes go through `api/client.ts` (only `fetch` caller) + a
  per-resource hook; extend `api/queryKeys.ts`.
