# Mounted read-only at /repos in the server container (FR40, NFR5).
# Register a project with root_path=/repos (or /repos/<subdir>).

This directory is a placeholder so `docker compose up` has a bind mount.
Put a git checkout here, or set PCS_REPOS_DIR to the host path you want indexed.
