# Review — T02 Requirements template file

- **Branch:** `task/T02-requirements-file` (2 commits, `a336bb6`, `d97ce69`), off `main` @ `0069fc7`
- **Reviewed:** 2026-09-10
- **`just check`:** ✅ pass — reproduced in the worktree (ruff, mypy `--strict` 52 files, pytest **77 passed** on real Postgres, eslint/tsc/vitest, `compose-lint`, `vite build`). Exit 0.
- **`just migrate`:** up path verified via testcontainers (`0001`→`0004_requirements_file`); downgrade code mirrors upgrade, not independently exercised.
- **Verdict:** ✅ **approved — merged.** One merge-ordering note (T04's migration), a few nits.

## Requirement coverage

| ID | Verified by | OK |
|----|-------------|----|
| FR16a (template file, `R-NNN`, managed token, prose untouched) | `pcs.requirements` (parser / template / service); `render_token`/`serialise` are line-surgical — only the token line + heading id are machine-edited | ✅ |
| D12 (created on `register_project`, two-way synced) | `register_project` calls `write_through_requirement_change`; `sync_requirements` tool + `POST /api/projects/{p}/requirements/sync`; `test_register_creates_the_template_file` | ✅ |
| D15 (file owns existence/prose, store owns status/links/history; 3-way merge; store wins status conflicts) | `_merge` — snapshot diff (`file_changed`/`store_changed`); file-only→file wins, store-only or both→store wins + token rewrite + `ReconciliationNote`; `test_ac22_status_changed_in_both_*` | ✅ |
| FR11 / D5 (archived kept in history, never resurrected) | new `STATUS_ARCHIVED` + `ACTION_ARCHIVE` + `archive_entry`; re-typed heading → reconciliation note, not un-archived; `test_ac22_deleted_block_is_archived_and_never_resurrected` (incl. the hand-re-add case) | ✅ |
| Malformed handling | `parse_requirements` never raises — per-block `error` + file-level `fatal_errors`; a bad block is skipped (last-good retained, `ok=False`), a bad *file* (dup id) applies nothing; `test_malformed_block_is_skipped_and_last_good_is_retained` | ✅ |
| AC18 | `test_ac18_add_in_store_writes_a_block_into_the_file`, `test_ac18_file_status_edit_flows_to_the_store`, `test_ac18_end_to_end_through_mcp_tools` | ✅ |
| AC22 | both tests above | ✅ |
| AC14a (server half) | `list_requirements` → `{requirements, done_count, total_count}`; `GET /api/projects/{p}/requirements`; UI half is T06 | ✅ seed |
| NFR5 | `resolve_requirements_path` rejects `..` in `PCS_REQUIREMENTS_FILE` | ✅ |
| NFR6 | `run_tool` wrapper; structured `requirements_sync` / `requirements_reconciliation` log lines | ✅ |

## ⚠️ Merge-ordering note (for T04)

T02's migration is **`0004_requirements_file`** with `down_revision = 0003_code_index`.
T04's branch has **`0004_index_semantic`**, also parented on `0003_code_index` —
**two heads once both merge.** T02 is merged first; **T04 must re-parent its
migration to `0005_*` with `down_revision = "0004_requirements_file"`** and
rebase on the new `main` before its review. Flagged in the T04 handback.

## Accepted deviations (from handoff)

1. **`archived` is a real `context_entries` lifecycle status** (touches T01's
   `context.service` status filters — minimal, all cited: `HIDDEN_STATUSES`
   frozenset replaces scattered `== STATUS_DELETED`). Necessary to distinguish
   AC22's "archived, in history, not removed" from `resolved`/`deleted`. ✅
2. **`files=` / `blocker=` tokens are store-owned** — seeded from the file on
   first sight, then rewritten from `linked_files` / `related_entry_id`. Matches
   D15 "store owns links". ✅
3. **Write-through runs a full `sync_requirements`** after every requirement tool
   call (keeps the snapshot fresh). Heavy (re-parse + merge + possible file write
   per mutation) but fine for a single-user tool. T06 should debounce UI edits. ⚠️ note
4. **`.env.example`** edited via an indirect shell reference (a `Read(.env.*)`
   deny rule blocks the tool) — hunk verified in review: `PCS_REQUIREMENTS_FILE`
   line is correct. ✅

## Nits
- **N1** — `alembic/env.py` gains another `# noqa: F401` (now 2, matching T03's
  side-effect-import pattern). Fix the pattern once in T09 (a ruff `per-file-ignores`
  or `_ = module`).
- **N2** — `write_through_requirement_change` swallows every non-`ProjectNotFoundError`
  exception (logged) so the store write survives a file problem. Correct per the
  spec, but a persistently unwritable file diverges silently until the next
  explicit `sync_requirements`; the returned `SyncReport.errors` is the only
  signal. T06 must surface `payload["requirements_file"]["errors"]`.
- **N3** — `sync_requirements` does file I/O while holding the project `FOR UPDATE`
  lock; a slow disk stalls other writes to that project. Fine for v1.

## Notes for dependents
- **T06:** `GET /api/projects/{p}/requirements` → `{requirements[], done_count, total_count}`
  for the view; `POST /api/projects/{p}/requirements/sync` → `SyncReport.as_dict()`
  for the manual-refresh control. `POST`/`PATCH /entries` and the requirement
  tools now return an extra `requirements_file` object — surface its `errors` and
  `reconciliations` (AC14a UI half). Entry JSON now carries `req_key`.
- **T04:** re-parent your migration to `0005` (see the note above).
- **T09:** the `# noqa` cleanup (N1).
